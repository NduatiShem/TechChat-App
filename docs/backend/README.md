# Backend reference (Laravel)

PHP snippets and backend integration guides were moved here from the app repo root during **Phase 0** cleanup.

| Folder | Contents |
|--------|----------|
| `guides/` | Integration guides — start with **`guides/MESSAGE_CONTROLLER_UPGRADE.md`** |
| `guides/` | Also: `CLIENT_MESSAGE_ID.md`, `MESSAGE_CONTROLLER_IDEMPOTENT.php`, `SOCKET_MESSAGE_BROADCAST.php` |

These files are **documentation only** — they are not executed by the React Native app. Apply changes on your Laravel backend project separately.
